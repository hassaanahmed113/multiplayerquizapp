import 'package:flutter/material.dart';

class AppColor {
  Color? headingColor = Colors.indigo[700];
  Color? textColor = Colors.white;
  Color? wrongColor = Colors.red;
  Color? correctColor = Colors.green;
  Color? blackColor = Colors.black;
}
